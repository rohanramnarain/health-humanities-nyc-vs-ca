# _config.yml
title: "NYC vs California — Health & Humanities"
description: "A minimal static site comparing public narratives, cultural institutions, and health access through a humanities lens."
# For a project site (not username.github.io), set your baseurl to "/<repo-name>"
baseurl: "/health-humanities-nyc-vs-ca"
url: "https://rohanramnarain.github.io"  # no trailing slash

theme: minima

# Safe, Pages-allowed plugins
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Navigation (minima supports header pages)
header_pages:
  - about.md

# Build settings
markdown: kramdown
kramdown:
  input: GFM
